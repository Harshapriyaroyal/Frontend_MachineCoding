// useContext is a React hook that allows you to consume context values without 
// wrapping components in Consumer components.
// It's essential for sharing data across component trees without prop drilling.

// useContext reads the value from a React Context and subscribes the component 
// to context changes.
// When the context value changes, the component automatically re-renders.